/*
    YAML.hpp

    Declaration file for YAML handling functions
*/

#ifndef YAML_HPP
#define YAML_HPP

#include <string_view>

// outputs the content of the given YAML file
void outputYAML(std::string_view filename);

#endif
